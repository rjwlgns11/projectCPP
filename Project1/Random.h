#pragma once
#include <stdlib.h>
#include <time.h>


//stdlib.h는 C언어의 표준 라이브러리 헤더 파일입니다.
//c언어의 작업을 진행할 때 빈번하게 많이 사용되는 파일로 다양한 기능을 가지고 있습니다.

//예시)
//1. 문자열에 대한 변환 문장 -> int , 문장 -> long과 같이 데이터 변환
//2. 의사 난수에 대한 생성(Random) 
//3. 메모리의 동적 할당(프로그램 내부에서 메모리를 추가하거나 제거하는 작업)
//4. 프로세스에 대한 제어(비정상 종료, 정상 종료 , 시스템 명령 등등)

//time.h C언어에서 시간 관련 작업을 진행할 때 사용할 헤더 파일