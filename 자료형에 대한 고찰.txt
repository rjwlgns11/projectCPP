unsigned int // unsigned == 음수없는 자료형 선언 입출력을 위해서는 %u를 사용한다
double a;//double 형 실수 최대 12자리 실수까지 입출력이 가능,입출력을 위해서는 %lfd를 이용한다
 long long a;//long long int형 정수 최대 900경 언저리,입출력을 위해서는 %lld를 이용한다
%o == 8진수 ex)012처럼 0xx로 선언된 변수는 8진수로 인식된다. 그리고 8,9값이 입력되지 않는다.


%x == 16진수 , %X ==16진수 대문자 입출력
%d == 10진수

-입력시 정수와 음수가 바뀌어 입력된다.

float형 계산시 주의 사항이 있는데 
정수/실수 , 실수/정수 (float)정수/정수, 정수/(float)정수 계산시 강제적으로 실수로 형변환된다.
이를 묵시적 데이터 형변환 == (float)를 붙이지 않을경우
또는 명시적 형변환 이라고 한다 == (float)를 붙였을 경우

논리낫 not !
1이상일경우 true(1)
0일경우 false(0)